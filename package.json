{
  "name": "@s2gprotocol/borsh-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "workspaces:test": "lerna run test --stream",
    "workspaces:clean": "lerna run clean --stream",
    "test": "vitest run -c vitest.config.ts"
  },
  "devDependencies": {
    "lerna": "^8.2.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "rimraf": "^3.0.2",
    "@types/node": "^18",
    "benchmark": "2.1.4"
  }
}